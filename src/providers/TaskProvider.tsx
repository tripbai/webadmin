'use client';

import { useEffect } from "react"

export default function TaskProvider({ children }: { children: React.ReactNode }) {
    useEffect(() => {
        
    })
    return <> {children} </>
}